/** Default polling interval for "realtime" data. */
export const POLLING_INTERVAL_MS = 5000;

/** Polling interval for running deployments */
export const RUNNING_DEPLOYMENT_POLLING_INTERVAL_MS = 2500;
